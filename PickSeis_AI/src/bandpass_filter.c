#include "utils/bandpass_filter.h"
#include <math.h>

void bandpassFilter(float* data, size_t length, double sampleRate, double freqmin, double freqmax) {
    // Dummy: Belum implementasi filter nyata, hanya placeholder
    // Untuk produksi, gunakan library DSP (misal: libiir, liquid-dsp, dsb)
    (void)data; (void)length; (void)sampleRate; (void)freqmin; (void)freqmax;
} 